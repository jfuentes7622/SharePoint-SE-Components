import { DynamicDataProvider, DynamicProperty } from '@microsoft/sp-component-base';
/**
 * PropertyPaneDynamicData component props.
 *
 * @alpha
 */
export interface IPropertyPaneDynamicDataProps {
    /**
     * List of all the properties to be configured by this dynamic data control.
     */
    entries: {
        /**
         * Name of the target property, as defined in the web part properties.
         *
         * @remarks
         * This needs to be a DynamicData property for the control to work correctly.
         */
        targetProperty: string;
        /**
         * User-friendly, localized label to identify the property name.
         */
        label?: string;
        /**
         * The DynamicProperty object for each configured property, if available.
         */
        dynamicProperty?: DynamicProperty<{}>;
    }[];
}
/**
 * Internal extension of the PropertyPaneDynamicData component props.
 * This is configured using real data from the web part to configure
 * proper usage of dynamic data.
 *
 * @internal
 */
export interface IConfiguredPropertyPaneDynamicDataProps extends IPropertyPaneDynamicDataProps {
    /**
     * The Dynamic Data provider for the web part.
     */
    dynamicDataProvider: DynamicDataProvider;
    /**
     * Default callback for the dynamic property.
     *
     * @remarks
     * This is a function that wraps the render() function.
     * This maps with the behavior from the deserialization of DD objects.
     */
    defaultCallback: () => void;
}
