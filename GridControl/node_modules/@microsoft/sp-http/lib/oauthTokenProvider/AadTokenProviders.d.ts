import AadTokenProvider from './AadTokenProvider';
/**
 * AadTokenProviders is used to retrieve preinitialized instances of the AadTokenProvider class.
 * This class should only be used within the SharePoint Framework.
 * @internal
 */
export default class AadTokenProviders {
    private static _spoCEWebAppSPProvider;
    private static _isInitialzed;
    /**
     * Initializes the AadTokenProviders. Must be called before fetching AadTokenProvider objects.
     * @internal
     */
    static _initialize(spoCEWebAppSPProvider: AadTokenProvider): void;
    /**
     * Retrieves a preinitialized version of the AadTokenProvider for communication with the SPO Client Extensibility
     * Web Application Service Principal. This application can be configured by granting permission scopes to the tenant's
     * instance of the Service Principal.
     * See more -> https://docs.microsoft.com/en-us/sharepoint/dev/spfx/use-aadhttpclient
     */
    static getConfigurable(): AadTokenProvider;
    private static _isInitializedCheck();
}
