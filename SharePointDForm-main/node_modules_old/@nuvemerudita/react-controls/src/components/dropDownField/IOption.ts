// Option interface definition
export interface IOption {
    value: string; // Unique identifier for the option
    text?: string; // Optional display text for the option
    disabled?: boolean; // Sets the option to a disabled state
    checkIcon?: React.ReactNode; // Check icon for selected options
    as?: "div"; // The HTML tag to render the option (e.g., "div")
  }
  