import ISuiteNavManagerConfiguration from './ISuiteNavManagerConfiguration';
import { IO365ShellRenderSettings } from './IO365Shell';
import ISuiteNavData from './models/ISuiteNavData';
import ISuiteNavModel from './models/ISuiteNavModel';
import ISuiteNavLink from './models/ISuiteNavLink';
/**
 * Data neccesary to construct an instance of SuiteNavManagerConfiguration.
 * @internal
 */
export interface ISuiteNavConfigurationData {
    currentUICultureName: string;
    disableSuiteNav?: boolean;
    settingsData?: ISuiteNavLink[];
    signoutUrl?: string;
    siteClientTag: string;
    systemUserKey: string;
    webServerRelativeUrl: string;
    /**
     * The webTemplateId is what kind of web this is, i.e. a team site or group site.
     */
    webTemplateId?: string;
}
/**
 * SuiteNavManagerConfiguration for the BaseApplication.
 * @internal
 */
export default class SuiteNavManagerConfiguration implements ISuiteNavManagerConfiguration {
    private _currentUICultureName;
    private _o365ShellRenderSettings;
    private _siteClientTag;
    private _systemUserKey;
    private _webServerRelativeUrl;
    private _hamburgerMenuCallback;
    private _suiteNavPostRenderCallback;
    private _isSuiteNavDisabled;
    private _settingsLinks;
    private _signoutUrl;
    private _webTemplateId;
    /**
     * Initializes a new instance of the SuiteNavManagerConfiguration.
     * @param suiteNavConfig - Data used to construct an instance of the SuiteNavManagerConfiguration.
     */
    constructor(suiteNavConfig: ISuiteNavConfigurationData);
    isSuiteNavDisabled(): boolean;
    /**
     * Function for disabling the suiteNav from loading
     */
    disableSuiteNav(): void;
    /**
     * Function for modifying the suite nav data that came back from the service.
     * @param suiteNavModel - Model is used to modify the resulting suiteNavData
     * @param themeDictionary - Color palette used to change the suiteNav theme colors
     * @returns the new SuiteNav data
     */
    modifySuiteNavData(suiteNavModel: ISuiteNavModel): ISuiteNavData;
    /**
     * {@inheritdoc ISuiteNavManagerConfiguration.getActionsMap}
     */
    getActionsMap(): {
        [key: string]: () => void;
    };
    /**
     * Gets the systemUserKey token.
     * @returns the systemUserKey or empty string
     */
    readonly systemUserKey: string;
    /**
     * Gets the cache token.
     * @returns the site client tag or empty string
     */
    readonly cacheToken: string;
    /**
     * Gets the o365ShellRenderSettings for rendering the SuiteNav.
     * @returns _o365ShellRenderSettings
     */
    readonly o365ShellRenderSettings: IO365ShellRenderSettings;
    /**
     * Property representing the culture name (e.g. "en-us")
     */
    readonly currentUICultureName: string;
    /**
     * Represents the web server relative url where we would ping the Suite Nav service.
     */
    readonly webServerRelativeUrl: string;
    hamburgerCallback: (() => void) | undefined;
    suiteNavPostRenderCallback: (() => void) | undefined;
}
