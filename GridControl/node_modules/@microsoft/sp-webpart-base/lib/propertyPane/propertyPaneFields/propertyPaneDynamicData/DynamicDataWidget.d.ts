/// <reference types="react" />
import * as React from 'react';
import { IDynamicDataWidgetProps, IDynamicDataWidgetState } from './IDynamicDataWidget';
export default class DynamicDataWidget extends React.Component<IDynamicDataWidgetProps, IDynamicDataWidgetState> {
    private _sources;
    constructor(props: IDynamicDataWidgetProps);
    render(): React.ReactElement<HTMLDivElement>;
    private _getDataSources();
    /**
     * Retuns a dropdown with id-title pairs of all DD sources that are currently available.
     */
    private _getDataSourcesDropdown();
    /**
     * Retuns a dropdown with id-title pairs of all availables properties for a specified DD source.
     */
    private _getDynamicDataProperties(sourceId);
    /**
     * Helper function for usage in hardcoded dynamic data. Not to be released publicly.
     * Copy-paste of _onPropertyChange because of that
     */
    private _onValueChange(targetProperty, value);
    private _onPropertyChange(targetProperty, option);
    private _getDataReference(targetProperty, property?);
}
