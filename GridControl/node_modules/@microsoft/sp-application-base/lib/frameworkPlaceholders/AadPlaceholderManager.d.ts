import { ServiceScope } from '@microsoft/sp-core-library';
import BaseApplication from '../BaseApplication';
/**
 * This class sets up an a placeholder for scenarios that token acquisition fails and will require
 * end user interaction.
 * @internal
 */
export default class AadPlaceholderManager {
    private static _logSource;
    private _aadPlaceHolderContent;
    private _eventHooked;
    /**
     * Sets up a placeholder (or alert if unavailable) for the token acquisition failure event.
     */
    setUpTokenAcquistionFailurePlaceholder(application: BaseApplication, serviceScope: ServiceScope): void;
    private _createAndRenderPlaceholder(placeholderProvider, eventArgs);
    private _renderTokenAcquistionPlaceholder(placeholderContent, eventArgs);
    private _renderTokenAcquistionAlert(eventArgs);
}
